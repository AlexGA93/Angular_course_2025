# Angular Course 2025 (v.17^)

- Node requirements:
    ```
    nvm install --lts

    ```
- Angular local CLI:
    ```
    npm install @angular/cli --save-dev
    ```
- Para mayor seguridad, renombrar el archivo environment.example.ts al clonar:
